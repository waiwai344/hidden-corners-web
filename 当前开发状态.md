# 当前开发状态

当前网络代理已配置成功，React + Express + TypeScript 版本已经可以安装依赖、类型检查、构建和运行。

## 当前主版本

- 前端：`client/`
- 后端：`server/`
- 数据库：`data/hidden-corners.sqlite`
- Excel 导入脚本：`data/import_from_excel.py`
- 数据库结构：`data/schema.sql`

## 启动方式

先导入数据库：

```bash
cd hidden-corners-web
npm run db:import
```

开发模式需要两个服务：

```bash
npm run dev:server
npm run dev:client
```

当前地址：

- 前端：`http://localhost:5173`
- 后端 API：`http://localhost:3001`

根目录也提供了一个合并启动命令：

```bash
npm run dev
```

## 已实现功能

- 地图页：使用 ECharts 注册真实中国 GeoJSON 地图，空间坐标点通过 `geo` 坐标系按经纬度叠加，支持滚轮缩放、拖拽漫游、按钮缩放和重置视角，悬停显示空间预览，点击进入详情。
- 图鉴页：支持关键词搜索、分类筛选、城市筛选、卡片/列表切换。
- 详情页：展示空间介绍、分类、平均评分、评价、活动。
- 用户系统：支持登录、注册、退出登录。
- 个人操作：支持想去、已打卡、添加评价。
- 游牧页：支持社区搜索、省份筛选、城市筛选。
- 我的页：展示身份档案、想去、已打卡、评价记录、个人轨迹点。

## 测试账号

```text
用户名：雪松与茶
密码：hc0001
```

其他 Excel 里的测试用户密码按 `hc0001`、`hc0002` 依次类推。

## 验证结果

已通过：

```bash
npm --prefix client run typecheck
npm --prefix server run typecheck
npm --prefix client run build
npm --prefix server run build
```

## 零依赖旧版本

`server.mjs` 和 `public/` 是网络不稳定时做的零依赖可运行备份。当前主版本已经切换到 `client/` + `server/`，旧版本只作为 fallback 保留。

如果要启动旧版本：

```bash
npm run legacy:dev
```

## 重置数据

网站中的想去、已打卡、评价会写入 SQLite。如果演示或测试后想恢复 Excel 原始数据，执行：

```bash
npm run db:import
```
